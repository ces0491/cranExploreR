# Offline unit tests for the app's pure functions.
# Run from the project root with:
#   Rscript -e 'testthat::test_dir("tests/testthat", stop_on_failure = TRUE)'
library(testthat)

test_dir("testthat", stop_on_failure = TRUE)
